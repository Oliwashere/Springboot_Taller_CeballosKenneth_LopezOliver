package com.example.fritos.repository;

import com.example.fritos.model.Senyora;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SenyoraRepository extends JpaRepository<Senyora, Long> {
}
